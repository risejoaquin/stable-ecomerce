# LAST VALIDATION

**Timestamp:** 2026-09-19T12:37:00-07:00
**Phase:** POST-LAUNCH 20 (PL20-03A: Cost Aggregation Hotfix: Attributable Cost Total Semantics)
**Branch:** `main`
**Base Commit:** `64696209a26efec83ae1ae7c3c3800801fa3d8ef`

## 1. Validation Suite Status

| Gate | Command | Result | Pass/Fail |
|---|---|---|---|
| TypeScript | `npm run lint` | 0 errors | PASS |
| Unit & API Tests | `npm test` | 122 tests passed across 4 files (105 in functional-quality-contracts) | PASS (122/122) |
| Production Build | `npm run build` | Vite client + esbuild server bundle | PASS |
| E2E Tests | `npm run test:e2e` | 20 tests passed across 3 spec files | PASS (20/20) |
| Secret Scan | `.\scripts\qa\security\scan-local-secrets.ps1` | 0 secrets detected | PASS |
| Resend Webhook Security | `.\scripts\qa\security\validate-resend-webhook-signature.ps1` | All checks pass | PASS |
| Legacy Upload Auth | `.\scripts\qa\security\validate-legacy-upload-authorization.ps1` | All checks pass | PASS |
| Security Baseline Report | `.\scripts\qa\security\validate-security-baseline.ps1 -Mode Report` | Report executed cleanly | PASS |
| Core Regression | `smoke-qa-release-e`, `smoke-mobile-ux-f`, `smoke-post-ux-c-hotfix-20`, `smoke-post-ux-c-hotfix-20-2` | 4/4 suites passing | PASS |
| Release Gate | `npm run qa:release` (`.\scripts\qa\validate-release.ps1`) | All 8 release gates passed | PASS |
| Git Diff Check | `git diff --check` | 0 whitespace or formatting errors | PASS |

## 2. Key Verified Behaviors

- Railway 192 MXN across 4 hosts with `shared_unallocated` is preserved in `shared_account_costs: { railway: 192 }` and `shared_unallocated_amounts: { railway: 192 }`.
- Attributable provider `amount` for Railway is explicitly `null` (not 192, not 48).
- Attributable provider `amount` for Stripe is `null` (PARTIAL fee structure).
- `measured_provider_total` strictly sums only providers with `measured_state === 'MEASURED'` and numeric amount (yielding 0 MXN for Supabase 0 + Resend 0).
- `total_estimate` evaluates to `null` whenever any provider is PARTIAL or unallocated; it is only a number when all four providers are MEASURED.
- Month period boundaries generated by `getMonthPeriodBounds(periodStr)` are valid inclusive calendar dates (`YYYY-MM-01` to actual last day of month).
- Resend free tier caveat does not contain unverified `"up to 3,000 emails/month"` volume claim.
- All 17 contract tests in `functional-quality-contracts.test.ts` pass cleanly.
- `finalScaleReady` evaluates strictly to `false`.
- PL20-03 remains ACTIVE; PL21 NOT STARTED.
