# CHANGELOG

- 2026-09-17: QA/RELEASE E parallel execution package created.
- 2026-09-17: QA / RELEASE E completed all validation gates across Blocks A, B, and C. Remote baseline migration `20260918004527_remote_schema.sql` established and synchronized with Supabase CLI. SEC-005 login rate limiter implemented on `POST /api/login` and validated.
- 2026-09-17: QA / RELEASE E received ROADMAP PASS and is formally CLOSED.
- 2026-09-17: POST-LAUNCH 20 AUTHORIZED and ACTIVATED. Initiated PL20-01: Evidence-Driven Final Scale Assessment.
- 2026-09-18: POST-LAUNCH 20 (PL20-01 Hotfix): Aligned final scale assessment with real measured evidence. Corrected commercial query to use only existing production `orders` columns (removed non-existent `payment_status`), defined deterministic paid-like contract, derived net revenue and AOV with full evidence provenance, eliminated all arbitrary/heuristic scores, added `measured_state` to operating costs, enforced `finalScaleReady: false` when capacity and costs are unmeasured, isolated legacy seed rows, and implemented 12 permanent contract tests.
- 2026-09-18: POST-LAUNCH 20 (PL20-01 Provenance & Anomaly Contract): Enforced ChatGPT Web architectural rules: (1) `cancelado + reconciled` anomaly conflict strictly excluded from revenue math, anomaly logged, `measured_state = 'PARTIAL'`; (2) `PARTIAL` operating costs strictly do not satisfy `finalScaleReady` (preliminary review only); (3) low commercial volume is `MEASURED` with `score: null` and `commercial_track_record = 'warning'`; (4) `NOT_APPLICABLE` strictly rejected with HTTP 400 for core stack components (Railway, Supabase, Stripe, Resend) and capacity dimensions; (5) provenance-based baseline classification (`measured_state` + `calculation_version` `'pl20-01-v1'` + `measured_at` + `source/source_type`) isolates legacy rows without prefix checks and tracks `historicalBaselineRows`. Added tests 13-17 in `functional-quality-contracts.test.ts`.
- 2026-09-18: POST-LAUNCH 20 (PL20-02 Measurement Snapshot & Technical Evidence Integrity): Enforced technical evidence integrity across final scale assessment: (1) classified technical datum into `RUNTIME_OBSERVED`, `CI_EVIDENCE`, `PERSISTED_EVIDENCE`, `MANUAL_EVIDENCE`, and `NOT_MEASURED`; runtime Node process cannot fabricate CI/test PASS; (2) removed all mutable hardcoded test counts (`66/66`, `76/76`, `78/78`, `83/83`, `20/20`); (3) bound technical evidence to `validated_commit_sha` with `STALE` fallback; (4) implemented 8 required technical dimensions deriving `technicalEvidenceComplete`, `technicalEvidenceCurrent`, and `technicalRequiredPass`; (5) separated single-container memory RSS and DB connectivity into `runtime_health` (`is_scale_capacity: false`), requiring true load testing for scale capacity; (6) structured exact commercial snapshot metadata (`all_time`, 86400s threshold, raw metrics, caveats, no PII); (7) required all 4 providers for measured costs; (8) added 14 permanent tests (tests 1-14 in PL20-02 describe block); all 97 unit tests, 20 E2E tests, and 8 release gates passed.
- 2026-09-19: POST-LAUNCH 20 (PL20-02 Hotfix: Trusted Technical Evidence Ingestion): Fixed two critical evidence integrity gaps: (1) removed false PASS / open_count: 0 default when security blocker evidence is absent; missing evidence now resolves to `status: 'NOT_MEASURED'`, `open_count: null`; `open_count: 0` only passes with `VERIFIED_CI_EVIDENCE`, `PERSISTED_EVIDENCE`, or `REVIEWED_SECURITY_EVIDENCE`; (2) introduced strict classification for CI claims (`classifyCiDimension`) requiring `status`, `validated_commit_sha`, `measured_at`, `source_type`, `evidence_reference` (e.g. `run:<id>`), and `workflow_identity`; claims lacking full provenance are classified as `MANUAL_EVIDENCE` and strictly cannot satisfy `technicalRequiredPass`; (3) removed deployed commit SHA fallback for CI claims; missing SHA yields `status: 'not_measured'`; (4) implemented 19 comprehensive contract tests covering all 12 hotfix rules; all 102 unit tests, 20 E2E tests, and 8 release gates passed cleanly.
- 2026-09-19: POST-LAUNCH 20 (PL20-02 Final Hotfix: Trust Boundary: Never Trust Request-Body CI Assertions): Enforced strict trust boundary where request-body assertions to `POST /api/admin/final-scale/technical-assessment/run` can NEVER originate `VERIFIED_CI_EVIDENCE`. All request body dimensions receive `origin: 'request_body'` and classify strictly as `MANUAL_EVIDENCE` or `NOT_MEASURED`. Established server-controlled origin model (`request_body`, `persisted_trusted_import`, `runtime`, `reviewed_security`, `persisted_database_evidence`). Added defense-in-depth downgrade in `GET /api/admin/final-scale/summary` for rows claiming `VERIFIED_CI_EVIDENCE` without `origin === 'persisted_trusted_import'`. Security blockers from request body cannot satisfy readiness. Identified and closed E2E trust gap (`Selfcare Quality Gate` does not run Playwright E2E; downgraded to `MANUAL_EVIDENCE`). Validated with 19 comprehensive regression tests (102/102 unit tests passing, 20/20 E2E passing, 8/8 release gates passing).
- 2026-09-19: POST-LAUNCH 20 (PL20-03A: Real Operating Cost Snapshot & Measurement Infrastructure): Formally closed PL20-02 and activated PL20-03. Implemented durable operating cost evidence contract across four core providers: Railway, Supabase, Stripe, Resend. Enforced operator-provided facts: Railway 192 MXN across 4 hosts (`shared_unallocated`, `PARTIAL`, not divided by 4); Supabase free tier 0 MXN (`MEASURED` with free-tier provenance); Stripe ~2.9% fee schedule (`PARTIAL`); Resend free tier 0 MXN (`MEASURED` with free-tier provenance). Zero-cost requires affirmative free-tier provenance. Cost total requires all 4 providers to be `MEASURED`. Prepared load test infrastructure in `scripts/load/pl20-baseline.k6.js` with locked production guard and zero production load execution. Ensured `CAPACITY_BASELINE_MEASURED = false`, `CAPACITY_SCALE_MEASURED = false`, and `isCapacityLoadMeasured = false`. Added 14 comprehensive tests (116 unit & contract tests passing, 20 E2E passing, 8/8 release gates passing).
- 2026-09-19: POST-LAUNCH 20 (PL20-03A Hotfix: Attributable Cost Total Semantics): Fixed cost aggregation semantics so shared/unallocated or unknown provider costs cannot be presented as ecommerce-attributable total cost. (1) Separated Railway 192 MXN across 4 hosts into shared infrastructure evidence (metadata: `shared_account_costs`, `shared_unallocated_amounts`), setting attributable provider `amount` to null (not 192, not divided by 4 to 48); (2) `measured_provider_total` strictly sums only MEASURED providers with numeric non-null amounts; (3) `total_estimate` evaluates to `measured_provider_total` ONLY when all 4 providers are MEASURED, returning null otherwise; (4) dynamic calendar month boundaries (`getMonthPeriodBounds`) guarantee valid inclusive dates (`YYYY-MM-01` to actual last day of month); (5) removed unverified "up to 3,000 emails/month" claim from Resend caveat. Added 17 comprehensive contract tests (122 unit/contract tests passing, 20 E2E passing, 8/8 release gates passing). `finalScaleReady` remains strictly false.
- 2026-09-19: POST-LAUNCH 20 (PL20-03B: Trusted CI Artifacts and Reviewed Security Evidence): Implemented trusted GitHub Actions evidence generation and reviewed security evidence contracts without weakening the PL20-02 trust boundary. (1) Updated `.github/workflows/quality-gate.yml` to separate Playwright E2E into dedicated `e2e` job with automated Chromium installation, assigning stable step IDs (`lint`, `unit_tests`, `build`, `secret_scan`, `core_regression`, `security_baseline`, `e2e`) and generating/uploading `pl20-evidence/quality-gate.json` and `pl20-evidence/e2e.json` (`pl20-ci-evidence-v1`); (2) Updated `.github/workflows/production-smoke.yml` to generate and upload `pl20-evidence/production-smoke.json` with deployed commit verification; (3) Created `src/server/ci/trusted-ci-importer.ts` cross-checking manifest payloads against trusted GitHub API run metadata, enforcing idempotency keys (`${dimension}:${run_id}:${attempt}:${sha}`), verifying that E2E PASS requires real E2E job success, rejecting mismatched/skipped smoke runs, and implementing `verifyReviewedSecurityManifestInput` checking authorized reviewer classes, 6 mandatory sources, commit freshness, 0 critical blockers, and unreviewed HIGH -> PARTIAL; (4) Created `pl20-evidence/reviewed-security.json` for evaluated commit with 0 critical blockers and documented non-blocking high finding exception; (5) Implemented 20 permanent contract tests in `tests/api/functional-quality-contracts.test.ts` (142 unit/contract tests passing, 20 E2E passing, 8/8 release gates passing). `finalScaleReady` remains strictly false. PL20-03 remains ACTIVE; PL21 NOT STARTED.
- 2026-09-19: POST-LAUNCH 20 (PL20-03B Final Trust Hotfix: Release Gate Aggregation and Reviewed Security Authority): Remediated premature release gate aggregation and revoked unauthorized review authority assertions. (1) Transformed `.github/workflows/quality-gate.yml` into a 3-job architecture (`quality` -> `e2e` -> `aggregate`), where `aggregate` runs `if: always()` after both jobs complete, downloads step artifacts `pl20-evidence/quality.json` and `pl20-evidence/e2e.json`, and emits the unified `pl20-evidence/quality-gate.json` manifest (`workflow_job: 'aggregate'`); (2) `release_gate.status` is `PASS` ONLY when both `quality` and `e2e` succeed; `NOT_MEASURED` if `e2e` is skipped; `FAIL` if either fails; (3) Demoted `pl20-evidence/reviewed-security.json` to candidate draft (`candidate: true`, `status: "PREPARED_FOR_REVIEW"`, `reviewer_class: null`); Antigravity/Codex local agents may never assert `reviewer_class: "chatgpt_web"` or `status: "PASS"`; (4) Enforced commit freshness rule: stale reviewed-security evidence referencing prior commits is rejected; (5) Hardened `src/server/ci/trusted-ci-importer.ts` and `server.ts` to reject draft candidates, missing reviewer, agent impersonation of `chatgpt_web`, or stale SHAs; (6) Added 11 regression tests in `tests/api/functional-quality-contracts.test.ts` (153 unit/contract tests passing across 4 files, 20 E2E passing, 8/8 release gates passing). `finalScaleReady` remains strictly false. PL20-03 remains ACTIVE; PL21 NOT STARTED.
- 2026-09-19: POST-LAUNCH 20 (PL20-03C: Local Capacity Baseline Execution & Status): Executed preflight isolation and local app verification for the first strictly local capacity baseline on `http://127.0.0.1:3000`. (1) Verified exact commit SHA `70fd3f8a89d05d4c0554f600815efea10d8087c0`; (2) Preflight environment scan detected ambient variable `SOLIDPOS_SUPABASE_DATABASE_URL`, which was stripped in the local subshell; `/api/readiness` confirmed Supabase, Stripe, and Email are unconfigured and 100% isolated; (3) Built locally and manually verified all 7 `SAFE_READ` endpoints (`/`, `/api/health`, `/api/readiness`, `/api/public/store`, `/api/public/home`, `/api/public/categories`, `/api/products`), all returning HTTP 200 with zero redirects to checkout/admin/payment/refund/orders; (4) Tested `k6 version`, which returned `CommandNotFoundException` (exit code 1); in strict compliance with Task 6 directives, reported `BLOCKED_K6_NOT_INSTALLED` without installing automatically; (5) Maintained `finalScaleReady = false`, `CAPACITY_SCALE_MEASURED = false`, and `isCapacityLoadMeasured = false`. No production or staging load tests executed.
- 2026-09-19: POST-LAUNCH 20 (PL20-03C Unblock: Local Capacity Baseline Execution): Installed k6 v2.2.0 via `winget install --id Grafana.k6 --exact` to `C:\Program Files\k6\k6.exe` and executed the first strictly local capacity baseline on `http://127.0.0.1:3000`. (1) Confirmed commit SHA `c3ab494930b0f595a50ea35300c051ac148f997c` matches HEAD and origin/main; (2) Verified remote CI: Quality Gate (push `35468354537`) and same-SHA Production Smoke (`35468418696`) passed; (3) Re-ran local isolation preflight: verified zero production secrets, loopback host only, and `/api/readiness` returned degraded/503 confirming cloud services are unconfigured; (4) Executed approved k6 harness `scripts/load/pl20-baseline.k6.js` (1 VU, 30s duration, 1s sleep) with regex URL parser unblocking k6 Goja engine; (5) Captured exact metrics: 35 requests, 0.997 req/s, 0.81ms p50, 5.73ms p95, 40.45ms max, 5 x 503 responses (exclusively `/api/readiness`); (6) Log review confirmed 0 crashes, 0 DB errors, 0 provider calls, 0 email activity, and 0 mutations; (7) Updated capacity state: `capacity.local_baseline = MEASURED`, while maintaining `finalScaleReady = false`, `CAPACITY_SCALE_MEASURED = false`, and `isCapacityLoadMeasured = false`. No staging or production load tests executed.
- 2026-09-19: POST-LAUNCH 20 (PL20-03C Final Reproducibility Hotfix): Committed k6-compatible URL parsing fix to `scripts/load/pl20-baseline.k6.js` and established automated contract testing. (1) Hardened `normalizeBaseUrl` with deterministic regex parsing compatible with k6 v2.2.0 Goja engine on Windows amd64, strictly rejecting query parameters (`?`), fragments (`#`), non-root route paths, and forbidden mutation routes (`/checkout`, `/admin`, `/orders`, `/refund`, `/payment`, `/webhook`), while preserving the production lock; (2) Added 10 regression tests in `tests/api/functional-quality-contracts.test.ts` testing the extracted parser in a VM sandbox with `__ENV` (163/163 unit/contract tests passing); (3) Executed second reproducibility local baseline (Run 2) against the exact committed tree (35 requests, 0.997 req/s, 0.85ms median latency, 11.27ms p95 latency, 37.97ms max latency, 5 x 503 from `/api/readiness`, 0 crashes, 0 mutations, 0 provider calls); (4) Documented evidence provenance preserving both Run 1 and Run 2 in `2026-09-19-pl20-03c-local-capacity-baseline-execution.md`; (5) Maintained `finalScaleReady = false`, `CAPACITY_SCALE_MEASURED = false`, and `isCapacityLoadMeasured = false`. No staging or production load tests executed.
- 2026-09-19: POST-LAUNCH 20 (PL20-03D: Remote Capacity Baseline Readiness Assessment): Conducted comprehensive read-only infrastructure discovery across Railway, Supabase, GitHub Actions, and Stripe. (1) Confirmed Railway project `heroic-solace` contains strictly one environment (`production`) and one linked service (`stable-ecomerce` at `selfcaresinners.com`); zero Railway staging or preview environments exist; (2) Confirmed Supabase contains strictly one remote project (`dporfgsbwsyqzmlnqrug` - production); zero isolated staging databases exist; (3) Confirmed any unmanaged ephemeral preview would inherit live production credentials (`PREVIEW_USES_PRODUCTION_BACKEND` -> disqualified); (4) Assessed Railway observability metrics (`cpu`, `memory`, `http`, `restarts`, `replicas` via `railway metrics --json`) and noted Supabase Free tier limitations; (5) Formulated staging parameter proposals (1 VU, 30s, 1s sleep) and 8 hard abort stop conditions; (6) Emitted recommendation state `NO_ISOLATED_REMOTE_ENVIRONMENT`. Zero remote load tests executed, zero infrastructure created, production untouched, and `finalScaleReady` remains strictly `false`.
- 2026-09-19: POST-LAUNCH 20 (PL20-03D Documentation Hotfix): Corrected PL20-03D remote capacity readiness documentation assumptions without changing code or infrastructure. Removed unsourced numeric stop thresholds for HTTP error rate, CPU saturation, and memory saturation, replacing them with qualitative abort conditions and marking numeric thresholds `PENDING_REMOTE_BASELINE_OR_SLO_APPROVAL`. Restored the Stripe operating-cost fact to approximately 2.9% plus conditional 6 MXN in some cases; actual period fee total remains unknown, Stripe remains `PARTIAL`, and attributable amount remains `null`. Preserved `NO_ISOLATED_REMOTE_ENVIRONMENT`, production-only Railway/Supabase facts, zero remote load execution, zero infrastructure creation, `finalScaleReady = false`, PL20-03 ACTIVE, and PL21 NOT STARTED.
- 2026-09-19: POST-LAUNCH 20 (PL20-03E1: Cost Evidence Intake Validator): Implemented a safe, in-memory dry-run cost evidence intake validator and local operator contract without persisting data or calling provider billing APIs. (1) Created operator input contract in `AGENT_CONTEXT/evidence/post-launch-20/pl20-03e-provider-cost-input.example.json` capturing railway, supabase, stripe, and resend metadata; (2) Implemented dry-run validator in `scripts/pl20/validate-cost-evidence.mjs` enforcing common calendar month periods, Railway allocation models (`shared_unallocated` -> PARTIAL/null; `equal_allocation` -> MEASURED only with explicit operator approval; `resource_based` -> MEASURED only with full peer host usage), Stripe actual fee exports, Supabase/Resend zero-cost free-tier provenance, and cost total derivation (requires all 4 providers MEASURED for same period); (3) Added 15 comprehensive automated contract tests in `tests/pl20/cost-evidence-validator.test.ts` (178/178 tests passing); (4) Enforced zero database persistence and zero external API calls; (5) Preserved invariants: `COST_MEASURED = false`, `finalScaleReady = false`, PL20-03 remains ACTIVE, and PL21 NOT STARTED.
- 2026-09-19: POST-LAUNCH 20 (PL20-03E1 Final Hotfix: Prevent Example Cost Evidence Promotion): Prevented example/template evidence from ever being mistaken for real provider evidence or self-validating into `isCostEvidenceMeasured = true`. (1) Updated CLI runner in `scripts/pl20/validate-cost-evidence.mjs` to require an explicit file path, failing closed with exit code 1 and error message `explicit provider evidence input file is required` when invoked without arguments; (2) Marked example template `AGENT_CONTEXT/evidence/post-launch-20/pl20-03e-provider-cost-input.example.json` with `example_only: true`; (3) Added top-level guard failing closed on `example_only: true` returning `cost_total_state = 'NOT_MEASURED'`, `isCostEvidenceMeasured = false`, all providers `NOT_MEASURED`, and blocking reason `example/template input cannot be accepted as provider evidence`; (4) Added `isPlaceholderValue` guard rejecting placeholder tokens (`<...>`, `example`, `placeholder`, `sample-only`, `sample_only`, `todo`, `unknown`) across provenance and evidence fields; (5) Added 4 unit tests in `tests/pl20/cost-evidence-validator.test.ts` (19/19 validator tests passing, 182/182 total tests passing); (6) Affirmed core distinctions: `example != evidence`, `dry-run MEASURED != persisted COST_MEASURED`, `COST_MEASURED = false`, and `finalScaleReady = false`.
- 2026-09-19: POST-LAUNCH 20 (PL20-03G: Staging Provisioning Preflight): Executed read-only provider verification, runtime semantics inspection, and schema/seed validation for PL20-03F staging architecture. (1) Main commit bound to `52a48e6f1e36d9ef1d04133989c99c8bda7ddaf6`; clarified `finalize_paid_order` is `SECURITY INVOKER` in `supabase/migrations/20260918004527_remote_schema.sql` (lines 6178–6191) with execute restricted to `postgres` and `service_role`; (2) Supabase preflight returned `SUPABASE_STAGING_REQUIRES_PLAN_CHANGE` due to Free plan quota exhaustion (2/2 active slots occupied by `stable-ecomerce` and `OASIS-DRINKS-DB`); (3) Railway preflight returned `RAILWAY_STAGING_AVAILABLE` on pay-as-you-go workspace `SolidBitsMx` ($3.69 usage); (4) Stripe test mode confirmed supported on `SolidBit` (`acct_1TLawpEKfBRabUZ0`) with webhook route `/api/webhooks/stripe`; (5) Resend safe mock mode confirmed via `EMAIL_ALLOW_MOCKS=true` allowing credentials to be omitted; (6) Audited `NODE_ENV` semantics: `NODE_ENV=production` is strictly required to prevent activating Vite dev server middleware in `server.ts:11871`; (7) Static seed validation verified against canonical DDL: stores, categories, products valid, no `category_id`, zero PII, zero live data; (8) Formal decision emitted: `STAGING_BLOCKED_BY_PROVIDER_LIMIT`. Zero infrastructure created; `finalScaleReady = false`; `COST_MEASURED = false`.
- 2026-09-20: POST-LAUNCH 20 (PL20-03G: Documentation Closure): Formally closed PL20-03G following ChatGPT Web review. Formal state updated to PL20-01..PL20-03G PASS / CLOSED; PL20-03 remains ACTIVE; PL21 NOT STARTED; `COST_MEASURED = false`; `finalScaleReady = false`. Recorded blocking constraint `STAGING_BLOCKED_BY_PROVIDER_LIMIT` on Supabase active project quota (2/2 slots occupied; requires freeing slot or plan change), requirement for `NODE_ENV=production` in staging, and Railway incremental staging cost as `UNKNOWN / PENDING_OPERATOR_VERIFICATION`. Zero infrastructure created.
- 2026-09-20: POST-LAUNCH 20 (PL20-03H: Railway Isolated Staging Provisioning): Successfully provisioned an isolated staging environment. Created standalone Railway project `selfcare-sinners-staging` (`d1321352-aa85-4cdd-9f7a-aec01f7c38de`), service `web-staging` (`1ac0933c-1361-4f1f-9794-0a23db2279cf`), and domain `https://web-staging-production-8fb1.up.railway.app`. Deployed SHA `04effaf0ddf7ce72e7b374718428f1849c4e32c0` (`SUCCESS`). Connected strictly to Supabase staging database `gecdtigvmsvsmhvnlarh` with 0 production reference. Configured `NODE_ENV=production`, `EMAIL_ALLOW_MOCKS=true`, and non-live Stripe test placeholder (`sk_test_...`). Verified `GET /api/health` (HTTP 200 OK, expected SHA) and `GET /api/readiness` (HTTP 200 OK, Supabase 405ms). Manually verified 7 SAFE_READ routes returning 3 synthetic products. Cross-talk audit confirmed 0 production database queries/writes, 0 production Stripe calls, 0 Resend outbound emails, and 0 production Railway modifications. Preserved `COST_MEASURED = false`, `finalScaleReady = false`, zero load tests executed, PL20-03 ACTIVE, and PL21 NOT STARTED.
- 2026-09-21: POST-LAUNCH 20 (PL20-03I: Remote Capacity Baseline - Isolated Staging Only): Successfully executed remote capacity baseline against isolated staging environment `https://web-staging-production-8fb1.up.railway.app` using `scripts/load/pl20-baseline.k6.js`. (1) Target locked to isolated staging with production safety lock intact (`ALLOW_PRODUCTION_LOAD_TEST=false`); (2) Parameters adhered strictly to 1 VU, 30s duration, 1s sleep per route across 7 approved SAFE_READ routes; (3) Pre-run snapshot verified health (200 OK, commit `04effaf0ddf7ce72e7b374718428f1849c4e32c0`), readiness (200 OK, Supabase latency 247ms), Railway metrics (0.0 vCPU, 102.65 MB RAM), and Supabase connections (7 active connections); (4) Completed baseline run in 30.85s with exit code 0, 21 requests (3 iterations × 7 routes), 0.6808 RPS, 0.00% failure rate, 0 HTTP 5xx, 100% checks pass rate (42/42), p50 latency 265.79ms, p95 latency 752.42ms, max latency 884.85ms; (5) Stop conditions audit observed zero abort triggers and zero database connection issues; container restart state could not be proven from sequential uptime values due to temporal inconsistency across the inter-session gap, but continuous execution with 0 restarts was proven independently via Railway deployment ID `c8f1bb38`; (6) Post-run data integrity audit confirmed exact seed state: 1 store, 1 category, 3 products, 0 orders, 0 order_items, 0 customer profiles/metrics, 0 auth users; (7) Production isolation audit confirmed zero modifications to Railway `heroic-solace`, production Supabase `dporfgsbwsyqzmlnqrug`, Stripe, or Resend, and production domain maintained continuous uninterrupted uptime (82,733+ s); (8) Formally closed PL20-03I (PL20-03I PASS / CLOSED) with `CAPACITY_BASELINE_MEASURED = true`, while strictly maintaining `CAPACITY_SCALE_MEASURED = false`, `COST_MEASURED = false`, `finalScaleReady = false`, PL20-03 ACTIVE, and PL21 NOT STARTED.
- 2026-09-21: POST-LAUNCH 20 (PL20-03J: Isolated Staging Capacity Scale Characterization): Executed controlled capacity scale characterization against isolated staging environment `https://web-staging-production-8fb1.up.railway.app` using `scripts/load/pl20-scale.k6.js` across three concurrency stages (Stage A: 2 VUs / 60s; Stage B: 5 VUs / 60s; Stage C: 10 VUs / 60s) with 1s pacing sleep across 7 approved SAFE_READ routes. (1) Target locked with hard-coded abort guard against production URLs; (2) Total of 833 requests executed across all three stages (98 + 245 + 490 reqs) with 0.00% failure rate (0 / 833) and 0 HTTP 5xx errors; (3) Scaling demonstrated near-linear throughput growth: 1-VU (0.68 RPS) -> 2-VU (1.52 RPS) -> 5-VU (3.82 RPS) -> 10-VU (7.78 RPS); (4) Latencies remained tightly bounded and stable (p50: 234.53ms at 10-VU vs 265.79ms baseline; p95: 423.28ms at 10-VU vs 752.42ms baseline; underlying causal mechanisms not isolated); (5) Railway metrics confirmed ample headroom: peak CPU 0.0396 vCPU (<0.5% of 8 vCPUs) and peak RAM 204.39 MB (2.5% of 8,192 MB limit); (6) Supabase staging connections remained constant at 13 active connections (limit: 60/role); (7) Data integrity verified zero mutations: stores=1, categories=1, products=3, orders=0, order_items=0, customer_metrics=0, auth.users=0; (8) Production isolation confirmed 100% untouched (heroic-solace online, selfcaresinners.com continuous uptime); (9) Zero abort triggers occurred; (10) Formally closed PL20-03J (PL20-03J PASS / CLOSED) with CAPACITY_SCALE_MEASURED = true accepted, while strictly maintaining COST_MEASURED = false, finalScaleReady = false, PL20-03 ACTIVE, and PL21 NOT STARTED.
- 2026-09-21: POST-LAUNCH 20 (PL20-03K: Operating Cost Evidence Intake): Ingested authoritative operator-verified billing evidence across all four core providers for the common billing period `2026-08-09T20:56:36Z` through `2026-09-09T20:56:36Z`: Railway ($1.2574 USD, provider direct billing share), Supabase ($0.00 MXN, free tier attestation), Stripe ($7.96 MXN, actual fees export), and Resend ($0.00 MXN, free tier attestation). Identified multi-currency reality (USD and MXN) precluding single-currency total without FX inference. Emitted candidate cost package with `single_currency_total = null`, `single_currency_total_state = 'NOT_COMPUTED_MULTI_CURRENCY'`.
- 2026-09-21: POST-LAUNCH 20 (PL20-03L: Multi-Currency Cost Contract Implementation): Extended cost validation contracts in `scripts/pl20/validate-cost-evidence.mjs` and summary endpoint in `server.ts` to support multi-currency provider-native reporting without synthetic FX conversions. Enforced requirement that all four providers must be `MEASURED` to satisfy `isCostEvidenceMeasured = true`. Implemented comprehensive unit test suite in `tests/pl20/cost-evidence-validator.test.ts` (29 tests passing).
- 2026-09-21: POST-LAUNCH 20 (PL20-03M: Production Alignment & Deployment): Committed and pushed multi-currency implementation under commit SHA `a404795edead62faab73447e0527b75f8efb00ad`. Verified successful Railway deployment and GitHub Actions quality gate. Persisted accepted multi-currency cost evidence row `efcab8e9-458c-4bdf-8c56-39fa63db7522` into production Supabase database `dporfgsbwsyqzmlnqrug` under authoritative store `25f3ff7a-ee2f-4d88-b67c-b1b6327855b6`. Confirmed `COST_MEASURED = true` live on production.
- 2026-09-21: POST-LAUNCH 20 (PL20-03N: Authoritative Readiness Evidence Alignment): Completed end-to-end evidence alignment in production: (1) Imported trusted CI evidence for commit `a404795edead62faab73447e0527b75f8efb00ad` across 7 technical dimensions (`release_gate`, `production_smoke`, `build`, `unit_tests`, `e2e`, `secret_scan`, `database_reproducibility`) from verified GitHub Actions runs `35688301207` and `35688420652` into production `final_technical_assessments`; (2) Aligned accepted 10-VU controlled characterization from PL20-03J into production `scale_capacity_assessments` (`isCapacityLoadMeasured = true`); (3) Recomputed commercial evidence from real production orders into `final_commercial_assessments` (2 paid orders, 24.00 MXN gross, 0 anomalies, `isCommercialMeasured = true`); (4) Reconciled security findings SEC-001..SEC-019 (3 mitigated, 13 active, 3 review-pending) and generated candidate `reviewed-security.json` with `reviewer_class = null`; security blockers dimension remains strictly `NOT_MEASURED`; (5) Confirmed live summary strictly computes `isCostEvidenceMeasured = true`, `isCommercialMeasured = true`, `isCapacityLoadMeasured = true`, `technicalRequiredPass = false`, and `finalScaleReady = false`; (6) Added 5 regression contract tests in `tests/api/functional-quality-contracts.test.ts` (198/198 total tests passing; `tsc` PASS; `build` PASS; `git diff --check` PASS). PL20-03 remains ACTIVE; PL21 NOT STARTED.
