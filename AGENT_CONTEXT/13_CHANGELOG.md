# CHANGELOG

- 2026-09-17: QA/RELEASE E parallel execution package created.
- 2026-09-17: QA / RELEASE E completed all validation gates across Blocks A, B, and C. Remote baseline migration `20260918004527_remote_schema.sql` established and synchronized with Supabase CLI. SEC-005 login rate limiter implemented on `POST /api/login` and validated.
- 2026-09-17: QA / RELEASE E received ROADMAP PASS and is formally CLOSED.
- 2026-09-17: POST-LAUNCH 20 AUTHORIZED and ACTIVATED. Initiated PL20-01: Evidence-Driven Final Scale Assessment.
- 2026-09-18: POST-LAUNCH 20 (PL20-01 Hotfix): Aligned final scale assessment with real measured evidence. Corrected commercial query to use only existing production `orders` columns (removed non-existent `payment_status`), defined deterministic paid-like contract, derived net revenue and AOV with full evidence provenance, eliminated all arbitrary/heuristic scores, added `measured_state` to operating costs, enforced `finalScaleReady: false` when capacity and costs are unmeasured, isolated legacy seed rows, and implemented 12 permanent contract tests.
- 2026-09-18: POST-LAUNCH 20 (PL20-01 Provenance & Anomaly Contract): Enforced ChatGPT Web architectural rules: (1) `cancelado + reconciled` anomaly conflict strictly excluded from revenue math, anomaly logged, `measured_state = 'PARTIAL'`; (2) `PARTIAL` operating costs strictly do not satisfy `finalScaleReady` (preliminary review only); (3) low commercial volume is `MEASURED` with `score: null` and `commercial_track_record = 'warning'`; (4) `NOT_APPLICABLE` strictly rejected with HTTP 400 for core stack components (Railway, Supabase, Stripe, Resend) and capacity dimensions; (5) provenance-based baseline classification (`measured_state` + `calculation_version` `'pl20-01-v1'` + `measured_at` + `source/source_type`) isolates legacy rows without prefix checks and tracks `historicalBaselineRows`. Added tests 13-17 in `functional-quality-contracts.test.ts`.
- 2026-09-18: POST-LAUNCH 20 (PL20-02 Measurement Snapshot & Technical Evidence Integrity): Enforced technical evidence integrity across final scale assessment: (1) classified technical datum into `RUNTIME_OBSERVED`, `CI_EVIDENCE`, `PERSISTED_EVIDENCE`, `MANUAL_EVIDENCE`, and `NOT_MEASURED`; runtime Node process cannot fabricate CI/test PASS; (2) removed all mutable hardcoded test counts (`66/66`, `76/76`, `78/78`, `83/83`, `20/20`); (3) bound technical evidence to `validated_commit_sha` with `STALE` fallback; (4) implemented 8 required technical dimensions deriving `technicalEvidenceComplete`, `technicalEvidenceCurrent`, and `technicalRequiredPass`; (5) separated single-container memory RSS and DB connectivity into `runtime_health` (`is_scale_capacity: false`), requiring true load testing for scale capacity; (6) structured exact commercial snapshot metadata (`all_time`, 86400s threshold, raw metrics, caveats, no PII); (7) required all 4 providers for measured costs; (8) added 14 permanent tests (tests 1-14 in PL20-02 describe block); all 97 unit tests, 20 E2E tests, and 8 release gates passed.
- 2026-09-19: POST-LAUNCH 20 (PL20-02 Hotfix: Trusted Technical Evidence Ingestion): Fixed two critical evidence integrity gaps: (1) removed false PASS / open_count: 0 default when security blocker evidence is absent; missing evidence now resolves to `status: 'NOT_MEASURED'`, `open_count: null`; `open_count: 0` only passes with `VERIFIED_CI_EVIDENCE`, `PERSISTED_EVIDENCE`, or `REVIEWED_SECURITY_EVIDENCE`; (2) introduced strict classification for CI claims (`classifyCiDimension`) requiring `status`, `validated_commit_sha`, `measured_at`, `source_type`, `evidence_reference` (e.g. `run:<id>`), and `workflow_identity`; claims lacking full provenance are classified as `MANUAL_EVIDENCE` and strictly cannot satisfy `technicalRequiredPass`; (3) removed deployed commit SHA fallback for CI claims; missing SHA yields `status: 'not_measured'`; (4) implemented 19 comprehensive contract tests covering all 12 hotfix rules; all 102 unit tests, 20 E2E tests, and 8 release gates passed cleanly.
- 2026-09-19: POST-LAUNCH 20 (PL20-02 Final Hotfix: Trust Boundary: Never Trust Request-Body CI Assertions): Enforced strict trust boundary where request-body assertions to `POST /api/admin/final-scale/technical-assessment/run` can NEVER originate `VERIFIED_CI_EVIDENCE`. All request body dimensions receive `origin: 'request_body'` and classify strictly as `MANUAL_EVIDENCE` or `NOT_MEASURED`. Established server-controlled origin model (`request_body`, `persisted_trusted_import`, `runtime`, `reviewed_security`, `persisted_database_evidence`). Added defense-in-depth downgrade in `GET /api/admin/final-scale/summary` for rows claiming `VERIFIED_CI_EVIDENCE` without `origin === 'persisted_trusted_import'`. Security blockers from request body cannot satisfy readiness. Identified and closed E2E trust gap (`Selfcare Quality Gate` does not run Playwright E2E; downgraded to `MANUAL_EVIDENCE`). Validated with 19 comprehensive regression tests (102/102 unit tests passing, 20/20 E2E passing, 8/8 release gates passing).
- 2026-09-19: POST-LAUNCH 20 (PL20-03A: Real Operating Cost Snapshot & Measurement Infrastructure): Formally closed PL20-02 and activated PL20-03. Implemented durable operating cost evidence contract across four core providers: Railway, Supabase, Stripe, Resend. Enforced operator-provided facts: Railway 192 MXN across 4 hosts (`shared_unallocated`, `PARTIAL`, not divided by 4); Supabase free tier 0 MXN (`MEASURED` with free-tier provenance); Stripe ~2.9% fee schedule (`PARTIAL`); Resend free tier 0 MXN (`MEASURED` with free-tier provenance). Zero-cost requires affirmative free-tier provenance. Cost total requires all 4 providers to be `MEASURED`. Prepared load test infrastructure in `scripts/load/pl20-baseline.k6.js` with locked production guard and zero production load execution. Ensured `CAPACITY_BASELINE_MEASURED = false`, `CAPACITY_SCALE_MEASURED = false`, and `isCapacityLoadMeasured = false`. Added 14 comprehensive tests (116 unit & contract tests passing, 20 E2E passing, 8/8 release gates passing).
- 2026-09-19: POST-LAUNCH 20 (PL20-03A Hotfix: Attributable Cost Total Semantics): Fixed cost aggregation semantics so shared/unallocated or unknown provider costs cannot be presented as ecommerce-attributable total cost. (1) Separated Railway 192 MXN across 4 hosts into shared infrastructure evidence (metadata: `shared_account_costs`, `shared_unallocated_amounts`), setting attributable provider `amount` to null (not 192, not divided by 4 to 48); (2) `measured_provider_total` strictly sums only MEASURED providers with numeric non-null amounts; (3) `total_estimate` evaluates to `measured_provider_total` ONLY when all 4 providers are MEASURED, returning null otherwise; (4) dynamic calendar month boundaries (`getMonthPeriodBounds`) guarantee valid inclusive dates (`YYYY-MM-01` to actual last day of month); (5) removed unverified "up to 3,000 emails/month" claim from Resend caveat. Added 17 comprehensive contract tests (122 unit/contract tests passing, 20 E2E passing, 8/8 release gates passing). `finalScaleReady` remains strictly false.
- 2026-09-19: POST-LAUNCH 20 (PL20-03B: Trusted CI Artifacts and Reviewed Security Evidence): Implemented trusted GitHub Actions evidence generation and reviewed security evidence contracts without weakening the PL20-02 trust boundary. (1) Updated `.github/workflows/quality-gate.yml` to separate Playwright E2E into dedicated `e2e` job with automated Chromium installation, assigning stable step IDs (`lint`, `unit_tests`, `build`, `secret_scan`, `core_regression`, `security_baseline`, `e2e`) and generating/uploading `pl20-evidence/quality-gate.json` and `pl20-evidence/e2e.json` (`pl20-ci-evidence-v1`); (2) Updated `.github/workflows/production-smoke.yml` to generate and upload `pl20-evidence/production-smoke.json` with deployed commit verification; (3) Created `src/server/ci/trusted-ci-importer.ts` cross-checking manifest payloads against trusted GitHub API run metadata, enforcing idempotency keys (`${dimension}:${run_id}:${attempt}:${sha}`), verifying that E2E PASS requires real E2E job success, rejecting mismatched/skipped smoke runs, and implementing `verifyReviewedSecurityManifestInput` checking authorized reviewer classes, 6 mandatory sources, commit freshness, 0 critical blockers, and unreviewed HIGH -> PARTIAL; (4) Created `pl20-evidence/reviewed-security.json` for evaluated commit with 0 critical blockers and documented non-blocking high finding exception; (5) Implemented 20 permanent contract tests in `tests/api/functional-quality-contracts.test.ts` (142 unit/contract tests passing, 20 E2E passing, 8/8 release gates passing). `finalScaleReady` remains strictly false. PL20-03 remains ACTIVE; PL21 NOT STARTED.
- 2026-09-19: POST-LAUNCH 20 (PL20-03B Final Trust Hotfix: Release Gate Aggregation and Reviewed Security Authority): Remediated premature release gate aggregation and revoked unauthorized review authority assertions. (1) Transformed `.github/workflows/quality-gate.yml` into a 3-job architecture (`quality` -> `e2e` -> `aggregate`), where `aggregate` runs `if: always()` after both jobs complete, downloads step artifacts `pl20-evidence/quality.json` and `pl20-evidence/e2e.json`, and emits the unified `pl20-evidence/quality-gate.json` manifest (`workflow_job: 'aggregate'`); (2) `release_gate.status` is `PASS` ONLY when both `quality` and `e2e` succeed; `NOT_MEASURED` if `e2e` is skipped; `FAIL` if either fails; (3) Demoted `pl20-evidence/reviewed-security.json` to candidate draft (`candidate: true`, `status: "PREPARED_FOR_REVIEW"`, `reviewer_class: null`); Antigravity/Codex local agents may never assert `reviewer_class: "chatgpt_web"` or `status: "PASS"`; (4) Enforced commit freshness rule: stale reviewed-security evidence referencing prior commits is rejected; (5) Hardened `src/server/ci/trusted-ci-importer.ts` and `server.ts` to reject draft candidates, missing reviewer, agent impersonation of `chatgpt_web`, or stale SHAs; (6) Added 11 regression tests in `tests/api/functional-quality-contracts.test.ts` (153 unit/contract tests passing across 4 files, 20 E2E passing, 8/8 release gates passing). `finalScaleReady` remains strictly false. PL20-03 remains ACTIVE; PL21 NOT STARTED.
- 2026-09-19: POST-LAUNCH 20 (PL20-03C: Local Capacity Baseline Execution & Status): Executed preflight isolation and local app verification for the first strictly local capacity baseline on `http://127.0.0.1:3000`. (1) Verified exact commit SHA `70fd3f8a89d05d4c0554f600815efea10d8087c0`; (2) Preflight environment scan detected ambient variable `SOLIDPOS_SUPABASE_DATABASE_URL`, which was stripped in the local subshell; `/api/readiness` confirmed Supabase, Stripe, and Email are unconfigured and 100% isolated; (3) Built locally and manually verified all 7 `SAFE_READ` endpoints (`/`, `/api/health`, `/api/readiness`, `/api/public/store`, `/api/public/home`, `/api/public/categories`, `/api/products`), all returning HTTP 200 with zero redirects to checkout/admin/payment/refund/orders; (4) Tested `k6 version`, which returned `CommandNotFoundException` (exit code 1); in strict compliance with Task 6 directives, reported `BLOCKED_K6_NOT_INSTALLED` without installing automatically; (5) Maintained `finalScaleReady = false`, `CAPACITY_SCALE_MEASURED = false`, and `isCapacityLoadMeasured = false`. No production or staging load tests executed.
- 2026-09-19: POST-LAUNCH 20 (PL20-03C Unblock: Local Capacity Baseline Execution): Installed k6 v2.2.0 via `winget install --id Grafana.k6 --exact` to `C:\Program Files\k6\k6.exe` and executed the first strictly local capacity baseline on `http://127.0.0.1:3000`. (1) Confirmed commit SHA `c3ab494930b0f595a50ea35300c051ac148f997c` matches HEAD and origin/main; (2) Verified remote CI: Quality Gate (push `35468354537`) and same-SHA Production Smoke (`35468418696`) passed; (3) Re-ran local isolation preflight: verified zero production secrets, loopback host only, and `/api/readiness` returned degraded/503 confirming cloud services are unconfigured; (4) Executed approved k6 harness `scripts/load/pl20-baseline.k6.js` (1 VU, 30s duration, 1s sleep) with regex URL parser unblocking k6 Goja engine; (5) Captured exact metrics: 35 requests, 0.997 req/s, 0.81ms p50, 5.73ms p95, 40.45ms max, 5 x 503 responses (exclusively `/api/readiness`); (6) Log review confirmed 0 crashes, 0 DB errors, 0 provider calls, 0 email activity, and 0 mutations; (7) Updated capacity state: `capacity.local_baseline = MEASURED`, while maintaining `finalScaleReady = false`, `CAPACITY_SCALE_MEASURED = false`, and `isCapacityLoadMeasured = false`. No staging or production load tests executed.

